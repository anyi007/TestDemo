package com.example.lql.testdemo.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.lql.testdemo.db.bean.shop.ShoppingCartBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SHOPPING_CART_BEAN".
*/
public class ShoppingCartBeanDao extends AbstractDao<ShoppingCartBean, Void> {

    public static final String TABLENAME = "SHOPPING_CART_BEAN";

    /**
     * Properties of entity ShoppingCartBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property UserId = new Property(0, Long.class, "UserId", false, "USER_ID");
        public final static Property Number = new Property(1, Integer.class, "Number", false, "NUMBER");
        public final static Property CommodityId = new Property(2, Long.class, "CommodityId", false, "COMMODITY_ID");
        public final static Property CommoditySizeId = new Property(3, Long.class, "CommoditySizeId", false, "COMMODITY_SIZE_ID");
    };


    public ShoppingCartBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ShoppingCartBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SHOPPING_CART_BEAN\" (" + //
                "\"USER_ID\" INTEGER," + // 0: UserId
                "\"NUMBER\" INTEGER," + // 1: Number
                "\"COMMODITY_ID\" INTEGER," + // 2: CommodityId
                "\"COMMODITY_SIZE_ID\" INTEGER);"); // 3: CommoditySizeId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SHOPPING_CART_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ShoppingCartBean entity) {
        stmt.clearBindings();
 
        Long UserId = entity.getUserId();
        if (UserId != null) {
            stmt.bindLong(1, UserId);
        }
 
        Integer Number = entity.getNumber();
        if (Number != null) {
            stmt.bindLong(2, Number);
        }
 
        Long CommodityId = entity.getCommodityId();
        if (CommodityId != null) {
            stmt.bindLong(3, CommodityId);
        }
 
        Long CommoditySizeId = entity.getCommoditySizeId();
        if (CommoditySizeId != null) {
            stmt.bindLong(4, CommoditySizeId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ShoppingCartBean entity) {
        stmt.clearBindings();
 
        Long UserId = entity.getUserId();
        if (UserId != null) {
            stmt.bindLong(1, UserId);
        }
 
        Integer Number = entity.getNumber();
        if (Number != null) {
            stmt.bindLong(2, Number);
        }
 
        Long CommodityId = entity.getCommodityId();
        if (CommodityId != null) {
            stmt.bindLong(3, CommodityId);
        }
 
        Long CommoditySizeId = entity.getCommoditySizeId();
        if (CommoditySizeId != null) {
            stmt.bindLong(4, CommoditySizeId);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public ShoppingCartBean readEntity(Cursor cursor, int offset) {
        ShoppingCartBean entity = new ShoppingCartBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // UserId
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // Number
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // CommodityId
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // CommoditySizeId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ShoppingCartBean entity, int offset) {
        entity.setUserId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNumber(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setCommodityId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setCommoditySizeId(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(ShoppingCartBean entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(ShoppingCartBean entity) {
        return null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
